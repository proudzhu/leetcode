#ifndef INTERVAL_HPP
#define INTERVAL_HPP

struct Interval {
    int start;
    int end;
    Interval() : start(0), end(0) {}
    Interval(int s, int e) : start(s), end(e) {}
};

#endif // INTERVAL_HPP
